{
  "buildCommand": "npm run build",
  "functions": {
    "api/[...slug].js": {
      "runtime": "nodejs20.x",
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/.*",
      "dest": "/api/[...slug]"
    }
  ]
}
